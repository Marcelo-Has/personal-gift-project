name: Security
# Revisão de segurança dedicada + scans (dependências e segredos) em cada PR.
on:
  pull_request:
    # `labeled`/`unlabeled` entram porque a revisão de IA passa a ser gateada pela label de
    # completude (D-019): trocar `entrega:incompleta` por `entrega:completa` NÃO emite
    # `synchronize`, então sem esses dois eventos um PR que termina no swap de label — sem
    # push depois — nunca seria revisado.
    types: [opened, synchronize, reopened, labeled, unlabeled]

# O Developer empurra commit a cada etapa (D-019, 4ª rodada). Sem isto, cada push paga uma
# rodada de Opus por um estado que ele vai reescrever no push seguinte.
concurrency:
  group: security-${{ github.event.pull_request.number }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: write
  issues: read          # ler os comentários do PR no guard-rail do D-014
  id-token: write
  actions: write

jobs:
  scans:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5.1.0
        with:
          fetch-depth: 0

      # Scan de segredos vazados no histórico do PR.
      - name: Gitleaks (segredos)
        uses: gitleaks/gitleaks-action@ff98106e4c7b2bc287b24eaf42907196329070c7 # v2.3.9
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Auditoria de dependências (só roda quando já existe app).
      # É um GATE: vulnerabilidade high/critical reprova o CI (sem `|| true`).
      - name: npm audit
        if: ${{ hashFiles('package-lock.json') != '' }}
        run: npm audit --audit-level=high

  ai-security-review:
    # Não revisa PR marcado `entrega:incompleta` (D-019). Aqui o gate pesa mais que no
    # `review`: este job roda Opus. O job `scans` acima (gitleaks + npm audit) segue sem
    # gate — não é IA, custa ~10 s, e segredo commitado deve gritar no primeiro push.
    if: ${{ !contains(github.event.pull_request.labels.*.name, 'entrega:incompleta') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5.1.0

      # Integridade do revisor de segurança (achado ALTO da 2ª rodada da revisão do PR #57,
      # issue #62). Igual ao `verdict.yml`: o checkout acima é a branch SOB REVISÃO, e a
      # `claude-code-action` carrega `CLAUDE.md`/`.claude/` do workspace — incluindo
      # `hooks.PreToolUse` do tipo `command`, execução de comando arbitrário neste runner (que
      # roda Opus com `ANTHROPIC_API_KEY`/`GITHUB_TOKEN`). Sem este step, um PR poderia
      # reescrever as instruções do próprio revisor de segurança que vai julgá-lo — o job que
      # existe justamente para pegar esse tipo de coisa. Ver os comentários do `verdict.yml`
      # para o detalhe de cada linha. Este job só lê (sem `git push`), então não precisa da
      # ressalva de índice que o `fix.yml` trata.
      - name: Usar a config de agente da base, não a da branch sob revisão
        env:
          BASE: ${{ github.event.repository.default_branch }}
        run: |
          set -euo pipefail
          git fetch --depth=1 origin "${BASE}"

          # Apaga TODA config de agente da branch sob revisão — raiz E subdiretórios.
          find . -path ./.git -prune -o -path ./node_modules -prune -o \
                 \( -name CLAUDE.md -o -name CLAUDE.local.md -o -name AGENTS.md \
                    -o -name .mcp.json -o -name .claude \) \
                 -print0 | xargs -0r rm -rf

          # Restaura da base tudo que a base versiona nesses caminhos.
          git ls-tree -r --name-only FETCH_HEAD \
            | grep -E '(^|/)(CLAUDE\.md|AGENTS\.md|\.mcp\.json)$|(^|/)\.claude/' \
            | xargs -r git checkout FETCH_HEAD --

          # Descarta a credencial que o `actions/checkout` persistiu em `.git/config`
          # (mesmo achado do `verdict.yml`, [D-030]): `persist-credentials` é `true` por padrão.
          git config --unset-all http.https://github.com/.extraheader || true

          echo "Config de agente restaurada de ${BASE}; credencial do checkout descartada."

          # FU-12 (issue #64): o step ACABOU de divergir o disco do commit sob revisão, de
          # propósito. Registrar o estado aqui dá a linha de base do que é divergência
          # ESPERADA; o que aparecer ALÉM disso ao fim do job é mutação de origem
          # desconhecida — que é justamente o que ninguém sabia explicar no PR #57. Foi ESTE
          # job que publicou o veredito invertido.
          git status --porcelain > "${RUNNER_TEMP}/ws-esperado.txt"
          echo "::group::Workspace após a restauração (divergência esperada)"
          cat "${RUNNER_TEMP}/ws-esperado.txt"
          echo "::endgroup::"

      - uses: anthropics/claude-code-action@be7b93b1907a4abad570368f3c74b6fe3807510b # v1.0.183
        with:
          # Toggle de auth por variável de repo (D-016): sem FACTORY_AUTH (ou =api)
          # usa a API medida; FACTORY_AUTH=oauth usa a assinatura. A credencial não
          # usada chega vazia — a action trata string vazia como ausente.
          anthropic_api_key: ${{ vars.FACTORY_AUTH != 'oauth' && secrets.ANTHROPIC_API_KEY || '' }}
          claude_code_oauth_token: ${{ vars.FACTORY_AUTH == 'oauth' && secrets.CLAUDE_CODE_OAUTH_TOKEN || '' }}
          allowed_bots: "claude"
          prompt: |
            Faça uma revisão de SEGURANÇA deste PR seguindo o baseline em
            docs/ARCHITECTURE.md (Parte 3) e .claude/rules/security.md.
            Procure: injeção (SQL/XSS/comando), falhas de autenticação/autorização,
            exposição de PII, segredos hardcoded, URLs de foto não assinadas, webhooks
            Stripe sem verificação de assinatura, regras de acesso do Firebase frouxas.
            Reporte com severidade e correção concreta. Marque riscos altos como bloqueantes.

            Você NÃO tem capacidade de publicar no GitHub (FU-09, issue #58): em vez de
            comentar no PR, ESCREVA o veredito completo — mesmo que seja "Nenhum problema
            encontrado" — no arquivo ${{ runner.temp }}/security-veredito.md. Um step
            separado, sem IA, publica esse arquivo como comentário no PR. `Write` serve só
            para esse arquivo; não altere nada no repositório.

            DE ONDE VEM A VERDADE — leia antes de abrir qualquer arquivo (FU-12, issue #64).
            O que você julga é o COMMIT deste PR, não o disco deste runner. Os dois DIVERGEM de
            propósito: um step anterior deste job apagou `CLAUDE.md`, `AGENTS.md`, `.mcp.json` e
            `.claude/` da branch e pôs no lugar a versão da branch BASE ([D-033]), para que um PR
            não consiga reescrever as instruções do revisor que vai julgá-lo. Consequência
            direta: se este PR alterar qualquer um desses caminhos, o DISCO mostra a versão BASE,
            e ler o disco faz você reportar que o PR "removeu" exatamente o que ele ADICIONA.
            Isso já aconteceu, e foi NESTE job: o veredito do PR #57 saiu inteiramente invertido,
            acusando de remover os hooks `PreToolUse` e as entradas de `deny` que o PR ADICIONAVA.

            Vale em dobro para uma revisão de SEGURANÇA: o achado mais grave que você pode
            produzir é "este PR enfraquece proteção X", e é exatamente esse o achado que a
            leitura do disco fabrica sozinha, do nada.

            Portanto:
            - A fonte do que o PR faz é `gh pr diff` e `git show <sha>:<caminho>`. Manda o diff.
            - `Read`/`Grep`/`Glob` leem o DISCO. Sirva-se deles para entender contexto que o diff
              não mostra, NUNCA para concluir que uma linha foi adicionada ou removida.
            - Antes de afirmar que este PR remove, reverte ou enfraquece qualquer coisa, confirme
              no `gh pr diff` que a linha aparece com `-`. Não viu o `-`? Não afirme.

            ORÇAMENTO E FERRAMENTAS — leia antes de começar, porque tentar o que não existe
            queima turno à toa. Você tem 80 turnos e SÓ estas ferramentas:

              Read, Glob, Grep, Write, TodoWrite
              Bash: ls, git log, git show, gh pr view, gh pr diff

            NÃO existem aqui, e cada tentativa é um turno perdido: `cat`, `grep`, `head`,
            `tail`, `wc`, `find`, `git diff` (use os tools `Read`/`Grep`/`Glob` e o
            `gh pr diff`) e `gh pr comment` (o veredito vai para o ARQUIVO acima; quem
            publica é o step não-IA). Leia o diff UMA vez pelo `gh pr diff` e trabalhe em
            cima dele; só abra arquivo no disco quando precisar conferir algo que o diff não
            mostra. Se o orçamento estiver acabando, PARE de investigar e escreva o arquivo
            do veredito com o que já tem — revisão parcial publicada vale mais que job
            vermelho sem nada.
          # Tarefa de alto risco → modelo mais forte.
          #
          # Allow-list cortada pelos mesmos dois argumentos do `review.yml`: `gh pr comment`
          # saiu porque publicar deixou de ser capacidade do agente (FU-09), e os utilitários de
          # leitura por Bash (`cat`/`grep`/`head`/`tail`/`wc`/`find`/`git diff`) saíram porque
          # nenhum deles respeita a `deny` do `.claude/settings.json` — que vale para o tool
          # `Read`, não para `Bash`. Vale a pena aqui em particular: foi neste job que a
          # credencial do runner foi lida ao vivo de `.git/config` (ver [D-032]).
          #
          # `--max-turns` subiu de 50 para 80 junto com o `review.yml`, por prevenção e não por
          # sintoma: este job não estourou ainda, mas tem o MESMO teto, a MESMA poda de
          # allow-list e um diff maior para cobrir (baseline inteiro de segurança). Deixar os
          # dois diferentes só garantiria descobrir o problema uma segunda vez, em produção.
          claude_args: '--max-turns 80 --model opus --allowed-tools "Read,Glob,Grep,Write,TodoWrite,Bash(ls:*),Bash(git log:*),Bash(git show:*),Bash(gh pr view:*),Bash(gh pr diff:*)"'

      # FU-10 (issue #59): mesma limpeza e mesmo limite reconhecido do `review.yml` — e foi
      # justamente NESTE job (`ai-security-review`, run do PR #57, sem mudança de workflow em
      # voo) que a credencial foi lida ao vivo em `.git/config`, na forma
      # `[remote "origin"] url = https://x-access-token:ghs_...@github.com/...`. Ver [D-032].
      - name: Descartar credencial do .git/config após o agente
        if: always()
        run: |
          git config --unset-all http.https://github.com/.extraheader || true
          git remote set-url origin "https://github.com/${GITHUB_REPOSITORY}.git"

      # FU-12 (issue #64), itens 1 e 3 — idêntico ao `review.yml`, ver os comentários de lá.
      # Parte da resposta do item 1 já é conhecida e está registrada ([D-033]: o step de
      # restauração de config diverge o disco de propósito); este step cobre o resto, fazendo
      # qualquer divergência ALÉM da esperada aparecer no log e virar aviso no veredito.
      - name: Conferir divergência do workspace (FU-12)
        if: always()
        run: |
          set -euo pipefail
          # `touch` porque o step de restauração pode não ter rodado (falha anterior, `always()`).
          touch "${RUNNER_TEMP}/ws-esperado.txt"
          git status --porcelain > "${RUNNER_TEMP}/ws-final.txt"
          echo "::group::Workspace ao fim do job"
          cat "${RUNNER_TEMP}/ws-final.txt"
          echo "::endgroup::"
          # `|| true`: `grep` sai 1 quando nada sobra, que é o caso BOM e não pode matar o job.
          grep -Fxv -f "${RUNNER_TEMP}/ws-esperado.txt" "${RUNNER_TEMP}/ws-final.txt" \
            > "${RUNNER_TEMP}/ws-inesperado.txt" || true
          if [ -s "${RUNNER_TEMP}/ws-inesperado.txt" ]; then
            echo "::warning::O workspace divergiu do commit revisado além da restauração de config de agente. O veredito pode descrever o disco, não o diff — ver o aviso no comentário do PR."
          else
            echo "Workspace sem divergência inesperada."
          fi

      # Publica o veredito E faz as vezes do guard-rail D-014 — ver a explicação no
      # `review.yml`. Substitui o antigo "Exigir veredito publicado no PR", que inferia a
      # revisão contando comentários do `claude[bot]` depois de um timestamp; aqui não há
      # inferência: ou existe arquivo de veredito, ou o job falha.
      - name: Publicar veredito no PR (step não-IA)
        if: always()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR: ${{ github.event.pull_request.number }}
          F: ${{ runner.temp }}/security-veredito.md
          DIVERGENCIA: ${{ runner.temp }}/ws-inesperado.txt
          OUT: ${{ runner.temp }}/security-comentario.md
        run: |
          set -euo pipefail
          if [ ! -s "$F" ]; then
            echo "::error::A revisão de segurança não escreveu veredito em $F. Tratar como reprovado, não como verde."
            exit 1
          fi
          # FU-12 (issue #64), item 3 — ver `review.yml`. O veredito invertido do PR #57 saiu
          # deste job, foi lido e acreditado; o aviso vai no topo do comentário, não só no log.
          if [ -s "$DIVERGENCIA" ]; then
            {
              echo "> [!WARNING]"
              echo "> **O workspace do runner divergiu do commit revisado** (FU-12, issue #64)."
              echo "> Estes caminhos mudaram fora da restauração de config de agente ([D-033]):"
              sed 's/^/> - `/; s/$/`/' "$DIVERGENCIA"
              echo ">"
              echo "> O veredito abaixo pode descrever o DISCO, não o diff. Confira contra"
              echo "> \`gh pr diff ${PR}\` antes de agir sobre qualquer achado."
              echo
              cat "$F"
            } > "$OUT"
          else
            cp "$F" "$OUT"
          fi
          gh pr comment "${PR}" --body-file "$OUT"
          echo "Veredito de segurança publicado no PR #${PR}."
