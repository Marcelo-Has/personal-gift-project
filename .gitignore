# Dependências
node_modules/
.pnpm-store/

# Build
build/
dist/
.svelte-kit/
.vercel/
.output/
.netlify/

# Ambiente e segredos — NUNCA commitar
.env
.env.*
!.env.example
*.pem
**/secrets*
serviceAccount*.json

# Fotos de teste do fixture de Pedido (FU-20) — foto real de pessoa é dado pessoal
# sensível e NUNCA entra no repositório; sem esta pasta o fixture gera placeholders.
src/lib/fixtures/photos-locais/

# Saída de execução do worker exportada para estudo (issue #151): documento do Firestore,
# fotos estilizadas, PDFs, logs. É resultado de uma corrida, não código — e o volume
# (dezenas de MB por execução) não pertence ao histórico do repositório.
artefatos-execucao/

# Config pessoal do Claude Code (não compartilhada)
.claude/settings.local.json
.claude/agent-memory-local/

# Sistema / logs
.DS_Store
*.log
coverage/

# Evidência visual do Visual Verification Loop (EV2.4): os PNGs de 375/768/1280 são gerados por
# rodada, sobem como artefato do PR e são lidos de lá pelo design-critic — não são código e não
# entram no histórico. O CAMINHO é convenção fixa (`.github/scripts/screenshots.mjs`).
artifacts/

# Playwright
test-results/
playwright-report/
blob-report/
playwright/.cache/
