# Este arquivo indica ao prettier para ignorar arquivos/pastas com output de build.
.DS_Store
node_modules
build
.svelte-kit
dist
package-lock.json
playwright-report
test-results

# Saída de execução exportada para estudo (issue #151) — gitignorada, não é código
artefatos-execucao/

# Documentação e specs mantêm formatação livre (não são código do app)
docs/
*.md

# Infra da fábrica / assets de produto — fora do escopo deste scaffold (F1-01)
.claude/
.github/
src/lib/product-skills/

# Regras do Firebase: linguagem própria (CEL), sem parser no prettier
*.rules
