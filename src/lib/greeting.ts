/**
 * Texto de placeholder da home page (Fase 0/1 — sem naming/identidade visual definidos).
 * A landing real é escopo de outra issue (F1-03).
 */
export function getPlaceholderTitle(): string {
	return 'Personal Gift Project';
}
